# PricePilot — Hermes environment template
# Copy to ~/.hermes/.env and fill in all blank values before starting the gateway.
# Auth note: Nimble uses Bearer token auth (not Basic); ClickHouse uses TLS on port 8443.

OPENAI_API_KEY=                            # LiteLLM key from organizers
OPENAI_BASE_URL=                           # LiteLLM proxy URL from organizers
OPENAI_MODEL=qwen35-35b
HERMES_MODEL=qwen35-35b
HERMES_YOLO_MODE=1
TERMINAL_ENV=local

TELEGRAM_BOT_TOKEN=
TELEGRAM_HOME_CHANNEL=                     # your Telegram user ID
GATEWAY_ALLOW_ALL_USERS=true

NIMBLE_API_KEY=
NIMBLE_BASE_URL=                           # Nimble realtime web API endpoint

CLICKHOUSE_HOST=
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=
CLICKHOUSE_TABLE=price_events
CLICKHOUSE_TRACKED_TABLE=tracked_products

SENSO_API_KEY=
SENSO_BASE_URL=

PRICEPILOT_DIR=                            # auto-set by setup.sh
